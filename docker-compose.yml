version: '3'
services:
  module.conteudo:
    build: 
      dockerfile: ./module.conteudo/dev.dockerfile
      context: .
    volumes:
      - ./module.conteudo:/usr/src/module.conteudo
    ports:
      - 7001:5001
    environment:
      NODE_ENV: development
